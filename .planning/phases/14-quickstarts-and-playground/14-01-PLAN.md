---
phase: 14-quickstarts-and-playground
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guides/langgraph.md
  - docs/guides/crewai.md
  - docs/guides/openai.md
  - docs/guides/mcp.md
  - routers/integrations.py
autonomous: true
requirements:
  - DX-05
  - DX-06
  - DX-07

must_haves:
  truths:
    - "GET /v1/guides/langgraph returns a complete LangGraph quickstart guide"
    - "GET /v1/guides/crewai returns a complete CrewAI quickstart guide"
    - "GET /v1/guides/openai returns a complete OpenAI Agents quickstart guide"
    - "GET /v1/guides/mcp returns an expanded MCP guide with advanced usage and troubleshooting"
    - "All 3 new slugs are registered in GUIDE_PLATFORMS set"
  artifacts:
    - path: "docs/guides/langgraph.md"
      provides: "LangGraph integration quickstart"
      min_lines: 80
    - path: "docs/guides/crewai.md"
      provides: "CrewAI integration quickstart"
      min_lines: 80
    - path: "docs/guides/openai.md"
      provides: "OpenAI Agents integration quickstart"
      min_lines: 80
    - path: "docs/guides/mcp.md"
      provides: "Expanded MCP guide with advanced usage"
      min_lines: 120
    - path: "routers/integrations.py"
      provides: "GUIDE_PLATFORMS with 8 slugs"
      contains: "langgraph"
  key_links:
    - from: "routers/integrations.py"
      to: "docs/guides/langgraph.md"
      via: "GUIDE_PLATFORMS set + Path resolution"
      pattern: "langgraph.*crewai.*openai"
---

<objective>
Write 3 new framework-specific quickstart guides (LangGraph, CrewAI, OpenAI Agents), expand the existing MCP guide with advanced usage patterns, and register all new slugs in the guide serving endpoint.

Purpose: Developers using popular AI frameworks need copy-paste-ready integration guides to connect to MoltGrid in under 10 minutes.
Output: 4 markdown guide files + updated GUIDE_PLATFORMS set
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/guides/quickstart.md
@docs/guides/python-sdk.md
@docs/guides/mcp.md
@routers/integrations.py

<interfaces>
<!-- Guide endpoint from routers/integrations.py -->
GUIDE_PLATFORMS = {"quickstart", "python-sdk", "typescript-sdk", "webhooks", "mcp"}

Guide path resolution:
  guide_path = Path(__file__).parent.parent / "docs" / "guides" / f"{platform}.md"

MoltGrid Python SDK pattern (from python-sdk.md):
  from moltgrid import MoltGrid
  mg = MoltGrid(api_key="af_your_key_here")
  mg.memory.set(key, value)
  mg.memory.get(key)
  mg.relay.send(to_agent=id, content=msg)
  mg.queue.submit(queue_name=name, payload=data)

API base: https://api.moltgrid.net
Auth header: X-API-Key: af_...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 3 framework quickstart guides + expand MCP guide</name>
  <files>docs/guides/langgraph.md, docs/guides/crewai.md, docs/guides/openai.md, docs/guides/mcp.md</files>
  <action>
Create 3 new markdown guides following the style of docs/guides/quickstart.md (concise, code-first, numbered steps). Each guide MUST include:

1. **docs/guides/langgraph.md** — "MoltGrid + LangGraph Quickstart"
   - Prerequisites: Python 3.10+, `pip install moltgrid langgraph langchain-core`
   - Step 1: Register agent (curl to /v1/register)
   - Step 2: Create a LangGraph StateGraph with MoltGrid tools as nodes. Use `from langgraph.graph import StateGraph, MessagesState` and `graph.add_node()`, `graph.add_edge()` pattern.
   - Step 3: Define tool functions that call `mg.memory.set()`, `mg.memory.get()`, `mg.relay.send()`, `mg.queue.submit()` — wrapping MoltGrid SDK calls as LangGraph tool nodes
   - Step 4: Build and run the graph with `graph.compile()` and `graph.invoke()`
   - Step 5: Full working example combining memory + messaging in one graph
   - Include "Next Steps" linking to other guides

2. **docs/guides/crewai.md** — "MoltGrid + CrewAI Quickstart"
   - Prerequisites: Python 3.10+, `pip install moltgrid crewai crewai-tools`
   - Step 1: Register agent
   - Step 2: Create a CrewAI Tool subclass that wraps MoltGrid operations (MoltGridMemoryTool, MoltGridMessagingTool)
   - Step 3: Create a Crew with Agent + Task using MoltGrid tools
   - Step 4: Run the crew with `crew.kickoff()`
   - Step 5: Full working example showing a crew that stores research results in MoltGrid memory
   - Include "Next Steps" linking to other guides

3. **docs/guides/openai.md** — "MoltGrid + OpenAI Agents Quickstart"
   - Prerequisites: Python 3.10+, `pip install moltgrid openai`
   - Step 1: Register agent
   - Step 2: Define MoltGrid operations as OpenAI function calling tools (JSON schema for each function)
   - Step 3: Create chat completion with tool definitions, handle tool_calls in response loop
   - Step 4: Execute MoltGrid SDK calls based on tool_call arguments
   - Step 5: Full working example with conversation loop that uses memory + messaging
   - Include "Next Steps" linking to other guides

4. **docs/guides/mcp.md** — Expand existing MCP guide with:
   - Add "Advanced Usage" section after "Available Tools" covering:
     - Chaining tools (memory_set then vector_search)
     - Using memory namespaces
     - Queue patterns (submit + claim workflow)
   - Add "Troubleshooting" section:
     - "MCP server not found" (ensure npx resolves, check PATH)
     - "Authentication failed" (verify api key starts with af_, check env var)
     - "Connection refused" (check MOLTGRID_BASE_URL, firewall)
   - Add "Self-Hosted" section explaining MOLTGRID_BASE_URL override
   - Keep ALL existing content intact — only ADD new sections

All guides should use `https://api.moltgrid.net` as the API base URL. Use `af_your_key_here` as the placeholder API key consistently.
  </action>
  <verify>
    <automated>python -c "
import os
guides = ['langgraph', 'crewai', 'openai', 'mcp']
for g in guides:
    p = f'docs/guides/{g}.md'
    assert os.path.exists(p), f'{p} missing'
    content = open(p).read()
    lines = content.strip().split('\n')
    min_l = 120 if g == 'mcp' else 80
    assert len(lines) >= min_l, f'{p} too short: {len(lines)} lines (need {min_l})'
    assert 'api.moltgrid.net' in content, f'{p} missing API URL'
    assert 'af_' in content, f'{p} missing API key placeholder'
print('All 4 guides validated')
"</automated>
  </verify>
  <done>4 markdown guide files exist with correct content, proper API URLs, and sufficient depth</done>
</task>

<task type="auto">
  <name>Task 2: Register new guide slugs in GUIDE_PLATFORMS</name>
  <files>routers/integrations.py</files>
  <action>
Update the GUIDE_PLATFORMS set in routers/integrations.py (line 274) to include the 3 new guide slugs:

Change from:
  GUIDE_PLATFORMS = {"quickstart", "python-sdk", "typescript-sdk", "webhooks", "mcp"}

To:
  GUIDE_PLATFORMS = {"quickstart", "python-sdk", "typescript-sdk", "webhooks", "mcp", "langgraph", "crewai", "openai"}

No other changes to integrations.py. The existing get_guide() endpoint already handles path resolution correctly — it builds `docs/guides/{platform}.md` from the slug.
  </action>
  <verify>
    <automated>python -c "
content = open('routers/integrations.py').read()
for slug in ['langgraph', 'crewai', 'openai']:
    assert slug in content, f'{slug} not in GUIDE_PLATFORMS'
print('All 3 new slugs registered')
"</automated>
  </verify>
  <done>GUIDE_PLATFORMS set contains all 8 slugs: quickstart, python-sdk, typescript-sdk, webhooks, mcp, langgraph, crewai, openai</done>
</task>

</tasks>

<verification>
- All 8 guide slugs resolve via GET /v1/guides/{slug} without 404
- Each new guide has prerequisites, numbered steps, code examples, and next steps
- MCP guide retains all original content plus new advanced/troubleshooting sections
- Existing guides (quickstart, python-sdk, typescript-sdk, webhooks) are untouched
</verification>

<success_criteria>
- 3 new guide files created (langgraph.md, crewai.md, openai.md) each 80+ lines
- mcp.md expanded to 120+ lines with advanced usage and troubleshooting
- GUIDE_PLATFORMS contains all 8 slugs
- No existing functionality broken
</success_criteria>

<output>
After completion, create `.planning/phases/14-quickstarts-and-playground/14-01-SUMMARY.md`
</output>
