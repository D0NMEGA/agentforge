name: Deploy API to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployed
        run: |
          echo "VPS self-deploys on push via /opt/moltgrid/deploy.sh"
          echo "Triggered by: $GITHUB_SHA"
